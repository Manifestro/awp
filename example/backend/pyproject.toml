[project]
name = "backend"
version = "0.1.0"
description = "A protocol-aware FastAPI reference backend for Agent Wake Protocol"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.34,<1",
]
